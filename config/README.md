master : hosts to run hadoop master
slaves : hosts to run hadoop slaves
satellites : hosts to run satellites
hduser : user to run hadoop
sateuser : user to run satellite
exclude-list.txt : rsync exclude list 
